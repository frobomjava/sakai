<?xml version="1.0"?>
<emailTemplates>
	<emailTemplate>
		<subject>${localSakaiName}：パスワードリセット要求</subject>
		<message>
		${localSakaiName}アカウントのパスワードのリセット依頼がありました。（${userEid}）

		${localSakaiName}のパスワードをリセットするには、次のリンクを使用してください：

		${url}
		
		セキュリティ上の理由から、このリンクは限られた期間のみ有効です。

		この変更をリクエストしていない場合は、このメッセージを無視してください。
		
		${localSakaiName}事務局
		${institution}
		</message>
		<locale></locale>
	</emailTemplate>
	<emailTemplate>
		<subject> New Password Request</subject>
		<message>${displayName}，您好：
请使用下面的链接来充值您在 ${localSakaiName} 的密码：

${url}
		
您的用户名是： ${userEid}

${localSakaiName} 维护组
${institution}
		</message>
		<locale>zh_CN</locale>
	</emailTemplate>
</emailTemplates>
